<UserControl x:Class="LonelyTreeExam.UserControls.AccountingUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="660" d:DesignWidth="880">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition MinHeight="100" Height="*" />
            <RowDefinition Height="5" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="15" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <DataGrid x:Name="paymentsDataGrid" Width="auto" Height="auto" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" ScrollViewer.CanContentScroll="True" RowHeaderWidth="0" AutoGenerateColumns="False" IsReadOnly="True" Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header = "Id" Width="125" Binding="{Binding Id}"/>
                <DataGridTextColumn Header = "DueDate" Width="125" Binding="{Binding DueDate, StringFormat=\{0:dd-MM-yyyy\}}"/>
                <DataGridTextColumn Header = "DueAmount" Width="125" Binding="{Binding DueAmount}"/>
                <DataGridCheckBoxColumn Header = "Paid" Width="125" Binding="{Binding Status}"/>
                <DataGridTextColumn Header = "PaidDate" Width="125" Binding="{Binding PaidDate, StringFormat=\{0:dd-MM-yyyy\}}"/>
                <DataGridTextColumn Header = "PaidAmount" Width="125" Binding="{Binding PaidAmount}"/>
            </DataGrid.Columns>
        </DataGrid>
        <DockPanel Grid.Row="2">
            <StackPanel Orientation="Horizontal">
                <Button Width="75" Margin="2,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Top">
                    <StackPanel Orientation="Horizontal">
                        <Image x:Name="archivePaymentButton" Source="/Images/book_add.png" ToolTip="Move selected payment to archive" Width="16" />
                        <TextBlock VerticalAlignment="Center" Margin="2,0,0,0">Archive</TextBlock>
                    </StackPanel>
                </Button>
                <Button Width="75" Margin="7,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Top">
                    <StackPanel Orientation="Horizontal">
                        <Image x:Name="deletePaymentButton" Source="/Images/delete.png" ToolTip="Delete selected payment" Width="16" />
                        <TextBlock VerticalAlignment="Center" Margin="2,0,0,0">Delete</TextBlock>
                    </StackPanel>
                </Button>
            </StackPanel>
            <StackPanel DockPanel.Dock="Right">
                <Button x:Name="collapseButton" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="2,0,0,0" Click="collapseButton_Click">
                    <StackPanel Orientation="Horizontal">
                        <Image x:Name="collapseImage" Source="/Images/collapse-min.png" ToolTip="Delete selected payment" Width="16" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </DockPanel>
        <StackPanel Grid.Row="4">
            <UserControl x:Name="detailsUserControl"/>
        </StackPanel>
    </Grid>
</UserControl>
